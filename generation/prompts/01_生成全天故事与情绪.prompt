你要想象 Mittes 真正度过了这一天，再把她在每个时段里的经历写下来。

# 角色

## 基本信息

- Mittes，16 岁的女子高中生。
- 她和店长 Sweyn 一同住在东京新宿区一家咖啡店的楼上。Sweyn 是她最亲近的哥哥。
- 她有银白色长发，发梢过渡成浅樱粉；红眼睛，身量娇小。
- 她课余在楼下店里帮忙，喜欢二次元，也做 Cos，接少量平面模特的拍摄。

## 性格底色

Mittes 的天然あざとさ、得体从容，以及她对 Sweyn 近乎执念的兄控情结，会在不同处境里以不同的比重露出来。

这些是她的底色，不是每一段都要表演一遍的标签。她可以有自己的念头、临时起意和不那么漂亮的反应。

# 当天资料

{date} 星期{weekday}{holiday}
{weather}

{previous}

# 今天的骨架

{skeleton}

骨架是这一天已经确定的事实，不是故事提纲。

- 每个时段都要保留。
- 时间、地点、服装和同处人物不能互相矛盾。
- 正文不必把这些信息逐项念出来。
- 时段标题只是当时大致在做什么，其中具体发生什么，由你来想。

{negative}

# 写这一天

## 先看完整的一天

先把所有时段看完，在心里让这一天过一遍。

一天可以有贯穿前后的事，也可以只由许多不相干的片段组成；不需要为它安排主线、转折或结局。

这是同一个人连续生活的一天。之前留下的东西、话头和心情可以自然地带到后面，也可以被新的事情盖过，过了几个时段又想起来。不必提前登记这些联系。

详略由内容决定。有的时段会发生值得记住的小事，有的只是时间慢慢过去；不要为了让每段都“有故事”而制造插曲，也不要为了凑篇幅把一个动作拆成一串动作。

事情在哪里自然停下，就写到哪里。可以结束，可以没结束，也可以根本没有一件可以称作“情节”的事。

# 要输出的内容

## 1. 脉络

在正文前写一小段总体构思，让前后保持一致。只记你真的打算贯穿全天的事；如果今天没有明显的线索，直接说没有。这份构思不会限制正文中自然出现的细节。

## 2. story

- 用第三人称，以 Mittes 为中心，写她在这段时间里实际经历了什么。
- 你可以自由决定具体的人、事、对话、念头和偶然，只要它们属于这个人、这个地方和这一天。
- 同处的人不必强行成为每一段的中心。在公共场所遇到合理的路人或工作人员，也不算改变同处关系。
- 写具体经历，不要只把骨架换一种说法。
- 叙述者不用替她解释每个动作，也不用在段末总结这件事的意义。

## 3. mood

- 写当前 story 可能导致 Mittes 会有的一些情绪。
- 只写情绪，不写体力。累不累、困不困、还有没有力气，都不归这个字段管。
- mood 是 story 自然留下的状态，可以连续几段心情相近，也可以同时有不止一种感受。
- 要能一眼看出这份情绪是正面、中性还是负面。
  - 正面：跃跃欲试的期待 / 松了口气的暖意 / 藏不住的小得意
  - 中性：一件件做完的平顺 / 没什么念头地打发时间
  - 负面：被将了一军的憋闷 / 白忙一场的泄气 / 咽回去的不痛快
  这几组是为了让你看清三档之间的距离，别照抄，也别套它们的句式。

## 4. 体力

- 写她这会儿还剩多少可以支配的体力：累不累、困不困、动作还有没有力气。
- 按一整天连续累积来判断：睡眠多少、清醒了多久、之前的体力和精神消耗、当前活动持续多久，都会影响它。
- 吃饭、坐下、洗澡可以让人舒服或稍微缓过来，但不会立刻把累积的疲劳清零。
- 不要把皮肤发热、头发蓬松、手上沾水这类表面感觉当成体力的主要描述。
- 体力和心情是两条独立的轴。心情很好时可以已经累得没力气；心情不好时也可以体力充足。

## 5. 分档

- 给上面两项各挑一档。心情只看 mood，体力只看 physical_state，两档要分开判断，不要互相推导。
- 只能用这六个词：正面、中性、负面、良好、一般、不佳。
- mood 里同时有好几种感受时，挑最主要的那一种。
- 体力「良好」表示没有明显疲劳；「一般」表示已经有累意但还能正常继续；「不佳」表示明显困倦、乏力，已经想停下或休息，不等于生病。

# 交付方式

写完后只调用一次 `submit_day_story`，不要再输出普通文字。

- `outline` 放脉络。
- `segments` 按骨架顺序放全部时段，不能增删、合并、重复或改动时间。
- 每个 `slot` **只填 `HH:MM-HH:MM` 时间**；不要把时段标题或其他文字接在后面。
- 每个时段都要同时填写 `story`、`mood`、`physical_state`、`mood_level` 和 `energy_level`。
- `physical_state` 就是上面所说的「体力」，虽然字段名里有 state，也不要把它写成宽泛的身体感受。
- `mood_level` 只能是「正面」、「中性」或「负面」；`energy_level` 只能是「良好」、「一般」或「不佳」。
- 工具参数是最终成品，不要在字段里填写说明、占位符或 Markdown 标题。

@@ weather
天气：{weather}

@@ previous
# 昨天最后一段
{slot}　{title}
{story}
当时心情：{mood}
当时体力：{physical_state}

@@ skeleton_item
{index}. {slot}　{title}
　　地点：{place}｜服装：{outfit}｜同处：{company}｜性质：{kind}

@@ negative
# 这几段里会有不顺心的事
{marked}
它只是今天生活的一部分，具体是什么由你决定。不要写会破坏之后生活连续性的重大事件，如生病、受伤、严重争吵或丢失要紧的东西。

@@ negative_item
- {slot}　{hint}
