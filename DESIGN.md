# 设计说明

面向想改这个插件、或者想借用其中思路的人。日常使用看 [README.md](README.md) 就够了。

- [要解决的问题：四个通道](#要解决的问题四个通道)
  - [换不换题，由 planner 定](#换不换题由-planner-定)
- [写 prompt 的两条原则](#写-prompt-的两条原则)
  - [原则二的一处例外：三条去 AI 味的规则](#原则二的一处例外三条去-ai-味的规则)
- [prompt 放在哪](#prompt-放在哪)
- [接入方式](#接入方式)
- [常见问题](#常见问题)

---

## 要解决的问题：四个通道

把日程做成一句话查表塞进上下文，角色会反复提起它："我现在正在洗碗呢，有什么事情吗？"
在 prompt 里写"不要提及"没用。要否定一件事，得先把它写进上下文，写进去本身就是提醒。
想让模型不复述，只能让上下文里根本没有这句话。

所以问题不在用注入还是用工具，在于传进去的东西能不能被复述：

| | 内容 | 出现时机 | 是否含活动名词 |
|---|---|---|---|
| 事实层 | 她具体在做什么（故事化文本） | 只有被问才出现（Tool） | 有，这是它的价值 |
| 状态层 | 她的心情，以及这如何影响说话 | 每轮都在（常驻注入）**——当前由 `[manner] enabled = false` 停用中** | 绝对没有 |
| 所在 | 她此刻在哪、这一段的行程 | 每轮都在（常驻注入） | 有，见下面的例外 |
| 谈资层 | 这段时间里值得说给人听的那一件事 | 接话版常驻注入；开口版只在 planner 调过工具的那一轮 | 有，唯一的例外 |

「正在洗碗」是可陈述的事实，模型有复述冲动。「你有点累、话短」没法复述，只能照做。
状态层能成立全靠这一点，而这一点又全靠不出现活动名词，所以 `manner` 和 `mood`
落盘前都要过一遍禁名词校验。

所在地点是个例外。地点是最顺口的复述内容（「我在 KTV 呢」），按上面的纪律不该常驻注入。
仍然这么做，是因为 planner 要在决定怎么演之前就知道人在哪，而工具只在「该聊她在干嘛」
时才被调用，那时候给已经晚了。知道这有风险，退路分两级：先去掉「所在」只留行程，
不够再整块撤回工具。

谈资层是有意开的口子。事实层有个先天缺陷：几乎没人会主动问「你在干嘛」，工具不被调用，
写得再好的 story 也用不上。谈资层给她一个出口，防复读的方式和状态层不同：
被用掉之后就停止注入，一件事只说一次。

这条由 `topic.stop_after_shared` 控制。关掉它就继续每轮注入——那是观察用的，
想看看她自己会不会反复提；此时防复读只剩注入文案在拦。

「说过」按**关联会话组**判定（`topic.linked_groups`，配的是群号，插件用
`ctx.chat.get_stream_by_group_id` 解析成 session_id，绝不自己算 hash）：组内任一个群里
说了，其余群都算说过。同一件小事在两个常驻群各讲一遍，比在同一个群讲两遍更容易被察觉。
关联只管「要不要注入」——记录仍按实际取材的那个会话落行，否则观察数据会缺一半。

### 状态层当前是停用的

`[manner] enabled = false`。停用不是删除：第三轮不再调 LLM、replyer 不再注入 A 块、
`/status expressions` 和管理页的重跑入口都不干活，但 `segments.manner` 列和库里已有的
165 条历史值原样保留，改回 `true` 就恢复，不需要重新生成。

停用的理由是实测下来它没有可测量的收益：按时段 kind 分组统计她两周的发言长度，
工作段（manner 写"简短利落"）和自由段（manner 写"愿意多聊"）的中位数分别是 11 和 12 字，
差一个字；只有睡眠段（中位 7 字）看得出差别，而那一档的归因也不干净——深夜群里本来就没人说话。
与此同时它每天要花一次 LLM 调用，还紧挨着 `reply_reference` 跟本轮的具体演绎指导抢方向盘。

先关掉观察一段时间，再决定是删是改。

### 谈资的两条通道

**这一节推翻了旧的「换不换题由 planner 定」设计**，起因是 2026-08-31 的实测：
同一条谈资在两个群抛出五次，四次刺耳，形态完全一致——先正常回一句，再另起一条硬转，
中间垫一个胶水词（话说 / 对了 / 先不说马尾 / 那个先不管）。最难看的一次是她自己要求
对方重发一张图、对方照做之后，她回了句「那个先不管」就开始讲补番。

病根有三条：材料在传递过程中变成了命令（planner 被要求把它写进 `reply_reference`，
而那条 reference 原样进 replyer 的上下文）；下命令的人看不见材料（planner 只知道
"有一件小事"，于是写出「我碰上件挺巧的小事，等我忙完再讲」这种预告）；
放行标准写的是"有没有空位"而不是"轮不轮得到"（原文是「这轮要是聊得松」）。

真正的判据来自一个更朴素的观察：**真人在群里说自己的事，绝大多数其实是接话**——
话题正好赶到那儿了。真正主动开口只有两种情形，而它们跟"聊什么"无关，只跟
"这会儿轮不轮得到我说"有关：事情**刚发生**（憋不住），或者她刚从一段不能说话的时间里
出来（**断点**，下班/下课/睡醒回到群里的第一句）。所以许可信号是**时间性**的，
不是内容性的。

由此拆成两条通道，各走各的条件和文案：

| | 通道一：接话 | 通道二：开口 |
|---|---|---|
| 形态 | 常驻注入，低权重 | `get_mittes_topic` 工具 |
| 条件 | 有 topic、还没被用掉 | 再加：在窗口期内、没有未合上的引用、取材次数没满 |
| 出口 | 只能"能直接回答对方或当例子时取用得上的那一点"，**明令禁止转话题** | 可以整条说这件事 |
| 位置 | system item 之后、所有聊天记录之前 | A（表达方式）之前、`reply_reference` 之前 |
| 判断者 | 无（接话不需要许可） | 代码定窗口，planner 看 description 自己决定调不调 |

判断权因此分成两半：**事实判断留给代码**（在不在窗口、有没有欠着别人一件事），
**氛围判断交给模型**（现在合不合适说自己的事）。旧设计是代码判断"合不合适"然后告诉
planner"合适"，那等于下命令——只要那句话在，planner 就倾向于用它。

**窗口**（`topic.pitch_channel`）：

- **新鲜期**：时段开始后 `fresh_minutes`（默认 30）分钟内。
- **断点期**：上一段的 `kind` 属于「不能玩手机」（睡眠/工作/上学）而本段属于「能」
  （自由/外出/通勤），且她在本段里还没开过口，且距时段开始不超过
  `breakpoint_minutes`（默认 60）。"回来的第一句"只有一次，她一开口就用掉了。

窗口很稀有。以 2026-09-01 为例：有 topic 的四段各自的前 30 分钟构成四个新鲜期，
全天合计约两小时；两个断点（09:00 睡醒、22:00 下班）落在的时段都没有 topic，窗口作废。
**预期是两三天说一条，不是每天一条。**

**未合上的引用**：本轮触发消息的 `reply_to` 指向她的某条消息——对方接住了她那条话，
球还在她这边，这一轮不许换题。实测一周里这种消息只占别人发言的 1~2%，是个很窄的守卫。
**不包含 @ 她**（4~7%）：引用是对方接住了她的话，@ 是对方开了个新话头，后者归 planner
本职管。

**上限** `max_pitches`（默认 3）按 (日期, 时段, 会话) 计，记的是"planner 主动取材几次"
而不是"注入几次"。它同时是成本闸：窗口期内工具每轮可见，模型可能连续调用，
每次调用都是一轮额外的 LLM 往返。

**令牌**：planner hook 在每轮开头清掉本会话的令牌，工具 handler 被调用时写入，
replyer hook 读取（不销毁，所以生成重试两次拿到的文案一致）。清 → 写 → 读这个顺序
让令牌的语义严格等于"本轮"，装的是**已发生的事实**而不是推断，所以不需要 TTL 去猜有效期。
读不到就走接话版——失败方向永远是更保守的那个。

**开口版为什么不再写「整条回复只说这件事」**：08-31 那五次一次都没执行。replyer 手上有
明确的目标消息和「请自然地回复」的收尾指令，这条约束要求它放弃本职任务，必然落败。
现在改成管接缝——禁掉那四个胶水词，并把形态从"叙述"压向"带情绪的半句"：
「早上那组黄得我难受，重修了一遍才顺眼」像憋不住说一句，
「我今天把早上那组照片重新裁了发到自己号上」像轮到我发言了。

**「用掉」的判据是取材，不是说出口。** 早先靠第二轮抽的 `topic_keys` 在回复正文里找
关键词，命中就算说过。那套已经整个取消：keys 抽的是 story 的书面词，而她开口是口语，
08-31 实测那条谈资在两个群注入 12 次一次都没命中，同一件事在同一个群讲了三遍。

现在的判据是 **planner 调过 `get_mittes_topic`** 这个确定事实——工具 handler 里直接
`mark_shared`。代价是取了材最后没说出来也算用掉，少一次开口机会；收益是绝不会重复说。
两种错的代价不对称，这个方向是有意选的。`topic_keys` 字段和库里的列都留着不删，
历史值有排查价值，新写入一律是空数组。

**已知不解决**：窗口开着、群里正有多人公共话题在密集来回、但没有一条消息引用她——
代码里没有任何东西能拦，全靠 description 那句「没有正在进行的话题需要接才调用」。
这是头号观察项；如果复发，第一步是把"公共话题在跑"（最近 5 分钟内除她之外 ≥2 个发言人
且平均间隔 <60 秒）作为硬拦加回来，而不是改 description。

---

## 写 prompt 的两条原则

这部分和本角色无关，做别的 LLM 生成功能大概也用得上。
两条都有实测数据支撑，数字来自同一份生成结果（20 个时段，两天）。

### 原则一：规则只能「排除」，不能「示范」

禁令排除掉一部分输出，剩下的仍然是无限的。写了「不要写重大事件」，小事还有无穷种写法。
示范不一样：列一串题材、给几个措辞、配一个完整例句，模型会在示范附近采样，
创作自由度当场坍缩。

| prompt 里写了什么 | 实测结果 |
|---|---|
| 配置里手写一份"倒霉事"清单 | 事件种类被限定在清单内，几周就开始重复 |
| 给「首句要出现角色名」配了一个完整例句 | 20 段里 13 段用同一个开头，其中一整天 10 段全是它 |
| 「打翻一杯水、找不到发圈、被熟客认出来……」 | 这些题材词在 20 段里命中 5 段 |
| 「踏实 / 懒洋洋 / 心里冒泡 / 跃跃欲试 / 被逗乐」 | 这些措辞在 20 段里命中 9 段，其中一条被逐字照抄 |

落笔时反例可以留，正例要吝啬。✗ 是排除，✓ 是种子，只在没有正例就说不清这条判据时给一个。
题材清单正向反向都别列。反向清单（「睡觉、洗澡、发呆、赶路就留空」）会替模型做完判断，
实测把「画油画」「涂指甲油」这些其实很有得讲的段落顺手扫掉了。

### 原则二：只写模型不可能知道的信息，不写它本来就会的能力

按原则一整理之后，story 的规则一度变成这样。全是禁令，符合原则一，但依然是错的：

```
- 长短句交错。一整段用逗号把动作串下来，读的人喘不过气
- 有人说话就写成引号里的原话，不要一律转成「说是……」
- 别每段都收在一句总结性的抒情上
```

一个称职的写手第一次读到「长短句交错」只会觉得莫名其妙。这类规则是在治症状。
病因是另一条约束：「100~150 字 + 有起因有推进有转折」。130 字塞完整三幕，
只能靠逗号把动作串起来。把字数放开、把任务说清楚，句法自己就顺了。

| 留，它不可能知道 | 删，它本来就会 |
|---|---|
| 人称/格式契约（下游字段要对齐） | 长短句、对话怎么写、别都收在抒情上 |
| 字数预算 | 「有起因、有推进、有一点小转折」 |
| 外部事实（地点 / 服装 / 同处） | 「不是定格一帧画面」 |
| 跨调用状态（它看不到全局） | 「把它写成这段时间的主线」 |
| 常驻注入的禁名词纪律 | |

按这把尺子，story 的规则从 20 行降到 4 行。

### 原则二的一处例外：三条去 AI 味的规则

按上面那把尺子，「叙述者不要跳出角色解释动作的用意」「每段结尾不要用总结性的感悟或升华」
「情绪写成行为，不写成形容词」都该删——都是写作技法，称职的写手本来就懂。但它们留在了
story 规则里（`generation/prompts/01_生成全天故事与情绪.prompt`），
因为**这不是能力问题，是默认档问题**：模型知道这些规则，不提醒就照犯。

分界线在有没有可治的病因。「长短句交错」当初该删，是因为它治的是症状——病因是
「130 字塞完整三幕」，字数放开句法自己就顺了。这三条没有这样的病因，模型在任何字数、
任何题材下都会犯。

实测同一天同一份骨架，加这三行前后：套语（忍不住 / 无意识地 / 眼睛一亮）14 次 → 2 次，
末句带收束标记 7/10 → 4/10，破折号 1 → 0，而正文字数几乎不受影响（279 → 259 字，无一段越界）。

代价不是零，两条都记下来：

- **堵一个口子，水从另一个口子出。** 套语和抒情收尾被堵住之后，软化副词
  （轻轻 / 渐渐 / 慢慢）从 4 次涨到 7 次。
- **prompt 挡不住全部。** 明写了不许解释动作，仍然漏出「含糊地哼哼了一声，
  像是在抗议这个周末居然也要醒过来」。这一条的形态是可正则检测的，
  真要归零得在 `_validate_state` 里加检查项——但那样这一段只会退回骨架底稿，
  因为不合格的段现在不补写。

顺带一条同源的发现，在角色块那边：**一句话的性格概述要留，成段的行为清单不要**。
拿掉概述，模型退回通用妹系模板（「哼了一声」「顺走一块马卡龙也毫不心虚」
「带着点撒娇的劲儿」，一天六处）；而把档案的行为清单搬进来 24 行，模板是压住了，
正文字数却从 279 挤到 207，十段里四段跌破下限。**prompt 的行数和正文的篇幅在抢同一份预算。**

---

## prompt 放在哪

三阶段生成的 prompt 在 `generation/prompts/` 下，文件名用“轮次 + 产物”标明用途。
**一次请求一个文件**：`01_生成全天故事与情绪.prompt` 就是全天生成那一次调用的全文。
Python 侧只负责填空，占位符用 `{名字}`，可选块和重复项写成 `@@ 片段名` 附在文件末尾。
加载器是 `generation/prompt_loader.py`，几十行。

注入 planner / replyer 的两小段、工具的说明与返回不在这儿，留在 `plugin.py`——
它们嵌在代码流程里，不是独立请求，抽出去反而要在两处之间跳着读。

角色块保留一句性格索引，不搬完整人设档案；一个文件就是一次完整请求，
共享的规则宁可各存一份也不抽公共段。设计理由留在本文档，不写进会原样发给模型的 `.prompt`。

---

## 接入方式

| 类型 | 名称 | 说明 |
|---|---|---|
| Tool | `get_mittes_schedule` | 事实层：某一时刻的时段与故事，被问才吐；未来只给安排 |
| Tool | `get_mittes_outfit` | 那一刻从头到脚的穿搭 |
| Tool | `get_weather` | 查询指定城市的实时天气和近 3 天预报 |
| Hook | `maisaka.planner.before_request` | 在「时间：」那条 item 后插入所在地点、心情、这一段的行程；并按窗口决定是否从 `tool_definitions` 里摘掉谈资工具 |
| Hook | `maisaka.replyer.before_model_request` | 谈资接话版放在 system 后/聊天记录前，开口版和表达方式一起放在 `reply_reference` 前 |
| Command | `/status *` | 调试命令，仅 operator |

注入全部走插件 hook，没有修改主程序源码。但有两处直接 import 主程序内部模块，
不走 SDK，上游重构会打断，同步版本时要重点回归：

- `LLMServiceClient` + `LLMGenerationOptions`（`generation/pipeline.py`）：为了钉死具体模型。
- `schema_version: 6` 的推理记录格式（`observability/prompt_preview.py`）：为了让调用出现在 WebUI 的推理过程页。

### 一天从凌晨 02:00 算起

骨架不按日历日切，按「入睡到入睡」切：02:00 起算，跨零点那段写成
`24:00-26:00`，归属当天。睡眠段放在开头。

按日历日切有两个毛病：

1. 同一个夜晚被劈成两半。写骨架的人在 24:00 收笔、00:00 重新起笔，
   于是一个晚上被设计了两次收尾，前后还容易倒序
   （`23:00 睡前护肤` → 次日 `00:00 卸妆`）。
2. 凌晨那段被告知「已清醒 0 小时」。那个数是从当天最后一个睡眠段之后算的，
   而凌晨段前面没有睡眠段。它存在的理由是给"累"一个客观锚点，
   结果在她一天里最累的时刻说她刚睡醒。

睡眠放开头之后第 2 条自动消失，那个计算函数一个字都不用改。

代码侧的关键是 `resolve_moment(moment) → (逻辑日, 逻辑日内分钟数)`，
凡是拿时刻查日程的地方都走它。直接用 `moment.date()` 会在每天午夜到 02:00
之间查错一整个日期，而且不报错，只会悄悄注入前一天的状态。

### 穿搭：名字进骨架，细节进工具

`character/wardrobe.toml` 存几套穿搭，每套一个名字加从头到脚的细节。
骨架的 `outfit` 只填名字。

名字跟着骨架进时段生成的 prompt，是 story 拿到的全部服装信息。细节只留在
`get_mittes_outfit` 工具里，绝不进 story 的 prompt。分开是因为从头到脚的清单喂进
story 会把它往服装说明书上带（原则一的应用）。

名字是写手拿到的唯一线索，所以怎么命名很要紧：

```
✓ 黑白女仆装 / 系黑丝带的白色连衣裙 / 有口袋的米白棉布连衣裙
✗ 便服 / 家居服 / 私服        抽象类别词，写手拿到等于没拿到
✗ 私服 A / 工作装 1           编号，直接毁掉整个设计
```

不做图层，「连衣裙 + 开衫」不另立一套。加一件外套算叙事事件，不算衣柜状态：
story 可以让她出门顺手抓件开衫，而工具回答「她穿着那条连衣裙」仍然成立。

### 生成节奏

每天 `run_at`（默认 12:00）生成次日全天，十来段，**一次调用**。

早先是一段一次调用、顺序执行，前一段的结果喂给后一段。那样每段的写手都看不到后面
会发生什么，只能把 250 字写成一个自洽的小故事，于是十段各自闭合、跨段因果为零。
实测同一天两种跑法：逐段版跨段线索 0 条，一次出全天 1~2 条，且十段里以角色名起手的
从 8 段降到 0 段。文风指标也顺带好转，破折号从 9/10 段降到 1/10。

代价是重试粒度：一次调用出全天，某一段写坏了没法单独补。曾经有一条「定向重写」的补写
路径（把全天骨架、脉络和前后段一起给模型，只换那一段），已移除——它是 `outline` 唯一的
消费方，而校验失败本来就是低频事件，为它养一整条 prompt + 调用链不划算。
现在不合格的段直接铺骨架底稿并原样报进批次结果，要内容就整天重来。

- 生成的是次日：12:00 才生成当天的话，00:00~12:00 那几段早就过去了
- 主生成跑完后，再跑一次话题提炼，全天十段一起出。抽取比创作简单，本可以用便宜模型，
  但 topic 会被注入到 replyer，写糙一点她开口就糙一点，所以现在填的是同一档
- 最后根据每段 story + mood 单独生成表达方式，一段失败不回头改正文
- 天气用预报不用实时，提前一天生成拿不到实时天气
- 冷启动时今天和明天缺哪天补哪天，期间用底稿顶着

### 负面事件

心情默认正向，但一条平坦的正向曲线同样假。所以负面情绪不靠"随机变差"制造，
而是每周随机安排 N 次"不顺心的事"，再让心情跟着这件事走。

排期只决定何时、多重，是什么由生成时的 LLM 依骨架现编。写死一份事件清单，
等于把她这辈子会遇到的倒霉事限定成那十几种（原则一的第一次应用）。

强度只有轻微和中等两档，没有"重大"。重大事件要影响好几天，
而承接链只覆盖相邻一两段，扛不住跨天的情绪线。

排期结果落在 `data/negative_schedule.json`，可以直接看、直接改。

### 可观测性

每次 LLM 调用都落盘到 WebUI 推理过程页，分类 `a_day_with_mittes`、会话 `schedule_batch`。
三种调用靠 `request.kind` 区分：`schedule_day`（全天生成）、
`schedule_topics`（话题提炼）、`schedule_expression`（表达方式）。

WebUI 那个页面没有上报接口，它是在扫 `logs/maisaka_prompt/` 目录树，
payload 格式（`schema_version: 6`）写死在主程序里，所以
`observability/prompt_preview.py` 自带一份拼装代码。

> **这是隐形耦合点。** 上游改了字段名，这里写出的文件会被静默忽略，页面空着且不报错。
> 同步主程序版本时要主动来查。

---

## 常见问题

### `base_task` 为什么要单独配？

插件直接用 `LLMGenerationOptions(model_name=...)` 钉死模型，基座任务实际只提供
`hard_timeout` 和统计管线。本部署里 `replyer` 的 `hard_timeout` 是 60 秒，`utils` 只有 20 秒，
而全天生成一次要吐十来段，实测 147~195 秒。所以指向了一个 240 秒的任务——
**只剩两成余量**，骨架变长或模型变慢都可能顶穿，顶穿了表现为整天退底稿。
换个部署就该按自己的配置重新挑。

### Command 的正则命名组，为什么不能写成同名形参？

因为主程序把 `(?P<day>…)` 整包塞进一个 `matched_groups` 字典，不拆成关键字参数。

写成 `async def cmd(self, day: str = "")` 不会报错，只是 `day` 永远是默认值。
**这类失效全程静默**：命令照常执行、照常返回成功，只是参数当没给过。
正确写法见 `plugin.py` 里的 `_group(kwargs, name)`。

### 问「你明天要干嘛」，她会说吗？

会说安排，不会说经过。次日日程提前一天就生成好了，但 story 写的是「发生了什么」。
事还没发生，照着念等于让她预言自己的一天，连细节带转折都说得出来。
所以查询工具遇到未来的时刻只给安排（几点在哪、做什么、穿什么），不给 story。

### 提前生成了一整周，`run_at` 的批次还会重跑吗？

不会。每日批次先查"明天有没有记录"，有就跳过，
所以接下来七天都会跳过，直到第八天才真正开跑。

注意判据是有没有记录，不是生成成功没有，这是为了防止模型不可用时反复重启空转一整轮。
副作用是：提前生成的某天如果十段全退回了底稿，它同样会被跳过，得手动补。

### 批次跑起来之后，能改插件目录里的文件吗？

不能。主程序对插件目录有文件监听热重载，改一个字就等于重载插件，
后台跑着的批次会跟着协程一起消失，**一行日志都不会留**。重启 bot 同理。

耗时命令是后台派发的，命令本身早就返回"已开始"了，从聊天记录上看不出来它还在跑。
要确认，看日志里有没有本插件的 LLM 调用记录。

### 换了骨架，旧日程会怎样？

骨架决定「这一天有哪些时段」。换骨架之后，库里的旧时段必须清掉，否则会静默出错：
实测某天变成 9 段旧 + 11 段新，批次报告写出「19/11 段完成」，
前端也会多显示几段查不到出处的日程。

清理只发生在 `flush()`，也就是只裁正在被重写的那一天。不在启动时全库对账，
换季替换骨架之后，那样会把整个上一季的归档抹掉。

顺带一条同类的：`days` 表原本存段数，而段数可以从 `segments` 数出来。
存一份就必然会漂，裁剪之后段数变了、存的数没变。所以直接去掉冗余列，查询时现数。

### 骨架为什么要手写，不让 LLM 自己排？

因为骨架是客观且不该每天变的事实：她住哪、几点上班、上班穿什么、跟谁在一起。
这些一旦交给 LLM，角色的生活会一天一个样，承接、疲劳累积、负面事件的场景约束
全都失去锚点。

LLM 只写这些事实今天具体表现成什么样。它绝不回写骨架，
所以删掉数据库随时能退回纯手写状态。

### 换季 / 换角色怎么办？

`schedule/skeleton.toml` 和 `character/wardrobe.toml` 一起整份替换，不要打补丁。
两份是配套的，骨架的 `outfit` 填的是衣柜里的名字，只换一边会对不上。

当前骨架覆盖的是日本的暑假：只有周三上午有学校安排（`08:30 通勤` + `10:00 上学`，
文化祭班级企划开会），其余六天没有 `kind = "上学"` 的时段。
开学后需要另写一套学期骨架（周一至周五白天替换为上学 + 通学，店里的班次相应往后压）。
**这件事没有触发机制，得靠人记着。**
